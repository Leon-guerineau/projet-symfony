{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ genre.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genre_crud_index') }}">back to list</a>

    <a href="{{ path('genre_crud_edit', {'id': genre.id}) }}">edit</a>

    {{ include('genre_crud/_delete_form.html.twig') }}
{% endblock %}
